# API base URL for the Rails wine prediction API
# Set this to the URL of your wine_prediction_api Rails server.
# For local development the default is fine.
VITE_API_BASE_URL=http://localhost:3000/api/v1
